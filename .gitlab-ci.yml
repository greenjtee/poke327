stages:
    - build
    
build-job:
    tags:
        - docker
    stage: build
    before_script: 
        - apt update
        - apt install -y build-essential libncurses-dev
    script:
        - make prod
